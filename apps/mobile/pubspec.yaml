name: tuki
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^8.3.1
  google_fonts: ^8.1.0
  http: ^1.6.0
  latlong2: ^0.10.1
  provider: ^6.1.5+1
  supabase_flutter: ^2.15.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/assets/
